---
phase: 04-display-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/App.tsx]
autonomous: true
---

<objective>
Display hold color indicator in climb list views.

Purpose: Allow climbers to visually identify climbs by hold color in their history.
Output: Color badge shown in Dashboard component for each climb with hold_color data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-types/01-01-SUMMARY.md
@.planning/phases/03-logger-integration/03-01-SUMMARY.md
@src/App.tsx
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add color badge component to Dashboard climb card</name>
  <files>src/App.tsx</files>
  <action>In the Dashboard component's climb card rendering (around line 87-218), add a hold color indicator:

1. Create a color mapping object constant at component level (similar to COLOR_CIRCUIT pattern from grades.ts):
   - Map 9 HoldColor values to hex values (red: #ef4444, green: #22c55e, blue: #3b82f6, yellow: #eab308, black: #18181b, white: #fafafa, orange: #f97316, purple: #a855f7, pink: #ec4899)

2. In the climb card render (after line 100, before the edit/delete buttons), add conditional rendering:
   - Check if climb.hold_color exists
   - If yes, render a colored circle indicator (w-4 h-4 rounded-full) with the mapped hex value
   - Add inline style for backgroundColor (better color accuracy than Tailwind)
   - Position it in the top section next to date/location info

3. Follow existing visual patterns:
   - Use similar spacing and layout to existing metadata (date, location)
   - Add a label "Hold Color" in the same font-mono uppercase style as other labels
   - Make it subtle but visible (matches the design aesthetic)

Reference the ColorSettings component (src/components/features/color-settings.tsx) for the same hex values used there.
</action>
  <verify>
1. pnpm typecheck passes (no TypeScript errors)
2. pnpm lint passes (no ESLint errors)
3. Color badge displays for climbs with hold_color data
4. Color badge doesn't render for climbs without hold_color (undefined/null)
5. All 9 colors display with correct hex values
  </verify>
  <done>
Hold color indicator displays in climb list for all climbs with color data, using correct hex values matching ColorSettings component.
</done>
</task>

<task type="auto">
  <name>Task 2: Style color badge to match design system</name>
  <files>src/App.tsx</files>
  <action>Refine the color badge styling for visual consistency:

1. Add a subtle border to the color circle (border-2 border-white/20) to match other UI elements
2. Add a small ring/shadow effect for better visibility (ring-1 ring-white/10)
3. Ensure the badge is aligned with other metadata (flex items-center gap-3 pattern)
4. Test on mobile viewport (375px) to ensure it doesn't cause layout shift
5. Verify the color badge is visible against the dark background (#0a0a0a)

The badge should be:
- Subtle but clearly visible
- Consistent with other UI elements (borders, spacing)
- Touch-friendly (if tappable in future for filtering)
- Accessible (good contrast against background)
</action>
  <verify>
1. Visual inspection: Color badge is visible and clearly colored
2. Mobile responsive: No layout shift on 375px viewport
3. Design consistency: Matches borders/spacing of other UI elements
4. Build succeeds: pnpm build completes
  </verify>
  <done>
Color badge is visually consistent with the rest of the Dashboard UI, clearly visible, and doesn't cause layout issues on mobile.
</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm typecheck passes with no errors
- [ ] pnpm lint passes with no warnings
- [ ] pnpm build succeeds for production
- [ ] Color badge displays for climbs with hold_color data
- [ ] Color badge doesn't break layout for climbs without hold_color
- [ ] All 9 colors render correctly
- [ ] Mobile layout verified (no overflow or shift)
</verification>

<success_criteria>

- Hold color indicator displays in climb list
- Colors match the ColorSettings component hex values
- Layout is consistent with existing UI patterns
- No TypeScript or build errors
- Mobile-friendly display
  </success_criteria>

<output>
After completion, create `.planning/phases/04-display-polish/04-01-SUMMARY.md`
</output>
