---
phase: 10.2-fix-mark-as-sent-button-text-color
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/features/climb-card.tsx]
autonomous: true
---

<objective>
Fix white text color on "Mark as Sent" button to match app's selection button pattern.

Purpose: The shadcn/ui Button's outline variant defaults to white text in dark mode, which is jarring compared to other selection buttons in the app (Climbs/Analytics navigation, Boulder/Sport discipline toggles) that use light gray text (text-[#aaa]).

Output: ClimbCard with "Mark as Sent" button using light gray text color (text-[#aaa]) instead of default white, matching the app's selection button aesthetic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase decisions:
## Phase 10.1 - Fix Mark as Sent Button Styling:
- Removed custom className from "Mark as Sent" button to let shadcn/ui outline variant provide proper styling
- Button now conforms to app's design system without white background overrides
- shadcn/ui outline variant handles emerald color theming correctly without manual overrides

## Phase 09 - Mark Failed as Succeeded:
- Emerald-500 theme colors for "Mark as Sent" button to indicate success action
- Used existing `useUpdateClimb()` hook for mutation

@src/components/features/climb-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add light gray text color to Mark as Sent button</name>
  <files>src/components/features/climb-card.tsx</files>
  <action>Add `text-[#aaa]` className to the "Mark as Sent" button to override shadcn/ui outline variant's default white text. Match the text color pattern used by selection buttons in the app (Climbs/Analytics navigation: text-[#aaa], Boulder/Sport discipline toggles: text-[#aaa]). This provides better visual consistency across the app - selection buttons use light gray text, not white.</action>
  <verify>pnpm typecheck passes (TypeScript compiles without errors)</verify>
  <done>"Mark as Sent" button uses light gray text (text-[#aaa]) instead of default white, matching app's selection button aesthetic</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm typecheck passes without errors
- [ ] Text color on "Mark as Sent" button is light gray (not white)
- [ ] Button maintains emerald color theming for border/hover states
</verification>

<success_criteria>

- "Mark as Sent" button uses light gray text color (text-[#aaa])
- Button styling matches app's selection button pattern (Climbs/Analytics, Boulder/Sport)
- No white text remains on the button
- TypeScript compiles without errors
- No changes to button behavior or functionality
  </success_criteria>

<output>
After completion, create `.planning/phases/10.2-fix-mark-as-sent-button-text-color/10.2-01-SUMMARY.md`
</output>
