---
phase: 16-add-version-number-to-footer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/ui/footer.tsx, src/App.tsx]
autonomous: true
---

<objective>
Add version number display to application footer for user reference and debugging.

Purpose: Display the current application version from package.json so users can track which version they're using and assist with debugging.

Output: New Footer component displaying version "v1.1.0" integrated into App layout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json

# App structure:
@src/App.tsx

# Existing UI patterns for styling reference:
@src/components/ui/offline-status.tsx
@src/components/ui/selection-button.tsx
@src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Footer component</name>
  <files>src/components/ui/footer.tsx</files>
  <action>Create Footer component that displays version from package.json. Use text-[#888] for version text to match App.tsx subtitle styling. Position as fixed at bottom with appropriate z-index to avoid overlapping with OfflineStatus. Export default Footer function.</action>
  <verify>Component exports successfully, TypeScript compiles</verify>
  <done>Footer component exists with version display</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Footer into App layout</name>
  <files>src/App.tsx</files>
  <action>Import Footer component and add it to the bottom of the Layout component's return, inside the main div but below the Outlet. Ensure it doesn't conflict with fixed positioning of other elements.</action>
  <verify>pnpm typecheck passes, Footer displays at bottom of all pages</verify>
  <done>Footer integrated into App layout showing version</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm typecheck passes
- [ ] pnpm lint passes
- [ ] Footer displays "v1.1.0" (or current version from package.json)
- [ ] Footer is visible on all routes (/, /analytics, /settings)
- [ ] Footer doesn't overlap with fixed elements (OfflineStatus, Logger button)
</verification>

<success_criteria>
- Footer component created in src/components/ui/footer.tsx
- Footer displays version number from package.json
- Footer integrated into App.tsx Layout component
- Version is visible across all pages
- No TypeScript or lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-add-version-number-to-footer/16-01-SUMMARY.md`
</output>
