---
phase: 10.1-fix-mark-as-sent-button-styling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/features/climb-card.tsx]
autonomous: true
---

<objective>
Fix white "Mark as Sent" button styling to conform with app's shadcn/ui design system.

Purpose: The button currently uses custom Tailwind classes that override shadcn/ui Button's built-in outline variant styling, creating an inconsistent white appearance. Fix by removing custom classes to let Button's outline variant properly inherit emerald theme.

Output: ClimbCard with "Mark as Sent" button that uses shadcn/ui Button outline variant without custom class overrides, conforming to app style.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/features/climb-card.tsx
@src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove custom Button classes from Mark as Sent button</name>
  <files>src/components/features/climb-card.tsx</files>
  <action>In ClimbCard component, find the "Mark as Sent" button (the Button that appears when climb.outcome === 'Fail'). Remove all custom className attributes that override shadcn/ui Button's styling: remove "className="w-full border-emerald-500/50 hover:bg-emerald-500/10 text-emerald-400 hover:text-emerald-300"". Keep only variant="outline" so the button uses shadcn/ui's built-in outline styling which properly handles emerald color theming. The outline variant already provides the correct border and hover behavior without manual overrides.</action>
  <verify>pnpm typecheck passes (TypeScript compiles without errors)</verify>
  <done>Button no longer has custom className overrides, uses shadcn/ui outline variant with proper emerald theming</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm typecheck passes with no TypeScript errors
- [ ] Button styling now consistent with other app buttons (no white background overrides)
- [ ] emerald color properly displays via outline variant
</verification>

<success_criteria>

- "Mark as Sent" button uses shadcn/ui Button outline variant without custom class overrides
- Button styling is consistent with app's design system
- TypeScript compiles without errors
- No changes to button behavior or functionality
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-fix-mark-as-sent-button-styling/10.1-01-SUMMARY.md`
</output>
